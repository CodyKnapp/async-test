{
  "name": "async-test",
  "version": "0.1.0",
  "description": "A wrapper to help with testing async methods",
  "author": "Cody Knapp <wcodyknapp@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/CodyKnapp/async-test"
  },
  "bugs": {
    "url": "https://github.com/CodyKnapp/async-test/issues"
  },
  "scripts": {
    "test": "mocha --opts mocha.opts",
    "release:tag": "git tag $(node -p -e \"require('./package.json').version\") && git push origin --tags",
    "release": "npm test && npm run build && npm run release:tag && npm publish"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.1.0",
    "sinon": "^4.5.0",
    "sinon-chai": "^3.0.0"
  }
}
